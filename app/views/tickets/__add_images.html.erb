<div class="container">
  <h2 class="mb-4">Envie seus tickets</h2>

  <%= simple_form_for :tickets, url: upload_images_tickets_path, method: :patch, html: { multipart: true, class: 'needs-validation', novalidate: true } do |f| %>
    <div class="tickets-container">
      <% @tickets.each do |ticket| %>
        <%= f.simple_fields_for :tickets, ticket do |ticket_form| %>
          <div class='order-ticket'>
            <div class="order-ticket-infos">
              <div class="form-group">
                <%= ticket_form.input :photo, as: :file, label: "#{ticket.ticket_type.type_of_ticket} #{ticket.id}", input_html: { accept: 'photo/*', class: 'form-control' } %>
                <%= ticket_form.hidden_field :ticket_id, value: ticket.id %>
              </div>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="mt-4">
      <%= f.submit 'Enviar arquivos', class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
